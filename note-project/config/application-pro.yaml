spring:
  datasource:
    url: jdbc:mysql://localhost:3306/note?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: rooth5lUs*dA*
  jpa:
    show-sql: false

logging:
  level:
    root: DEBUG

app:
  log:
    level:
      app: INFO
      athena: WARN
      framework: ERROR
    threshold:
      console: DEBUG
      all: DEBUG
